---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-gradle-mi-knath-java-11-2                  # Ingress name
  labels:
    app: test-gradle-mi-knath-java-11-2
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "test-gradle-mi-knath-java-11-2.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: test-gradle-mi-knath-java-11-2               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
